{
  "id": "{{randomValue length=3 type='NUMERIC'}}",
  "name": "{{jsonPath request.body '$.name'}}",
  "author": "{{jsonPath request.body '$.author'}}",
  "publication": "{{jsonPath request.body '$.publication'}}",
  "category": "{{jsonPath request.body '$.category'}}",
  "pages": "{{jsonPath request.body '$.pages'}}",
  "price": "{{jsonPath request.body '$.price'}}"
}